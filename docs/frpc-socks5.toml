# frpc Production Configuration Template
#
# Field order follows conf/frpc_full_example.toml

# Optional unique identifier for this frpc instance.
# clientID = "your_client_id"
# your proxy name will be changed to {user}.{proxy}
user = "your_name"
generateUserFromHardware = true

# group is global but used only in socks5_relay (overridden by TLS cert OU when cert is configured)
group = "group"

# A literal address or host name for IPv6 must be enclosed
# in square brackets, as in "[::1]:80", "[ipv6-host]:http" or "[ipv6-host%zone]:80"
# For single serverAddr field, no need square brackets, like serverAddr = "::".
serverAddr = "0.0.0.0"
serverPort = 7000

# STUN server to help penetrate NAT hole.
# natHoleStunServer = "stun.easyvoip.com:3478"

# Decide if exit program when first login failed, otherwise continuous relogin to frps
# default is true
loginFailExit = false

# console or real logFile path like ./frpc.log
log.to = "/var/log/frp/frpc.log"
# trace, debug, info, warn, error
log.level = "info"
log.maxDays = 3
# disable log colors when log.to is console, default is false
log.disablePrintColor = false

# auth.method = "token"
# auth.additionalScopes specifies additional scopes to include authentication information.
# Optional values are HeartBeats, NewWorkConns.
# auth.additionalScopes = ["HeartBeats", "NewWorkConns"]

# auth token
auth.token = "12345678"

# alternatively, you can use tokenSource to load the token from a file
# this is mutually exclusive with auth.token
# auth.tokenSource.type = "file"
# auth.tokenSource.file.path = "/etc/frp/token"

# oidc.clientID specifies the client ID to use to get a token in OIDC authentication.
# auth.oidc.clientID = ""
# oidc.clientSecret specifies the client secret to use to get a token in OIDC authentication.
# auth.oidc.clientSecret = ""
# oidc.audience specifies the audience of the token in OIDC authentication.
# auth.oidc.audience = ""
# oidc.scope specifies the permissions of the token in OIDC authentication if AuthenticationMethod == "oidc". By default, this value is "".
# auth.oidc.scope = ""
# oidc.tokenEndpointURL specifies the URL which implements OIDC Token Endpoint.
# It will be used to get an OIDC token.
# auth.oidc.tokenEndpointURL = ""

# oidc.additionalEndpointParams specifies additional parameters to be sent to the OIDC Token Endpoint.
# For example, if you want to specify the "audience" parameter, you can set as follow.
# frp will add "audience=<value>" "var1=<value>" to the additional parameters.
# auth.oidc.additionalEndpointParams.audience = "https://dev.auth.com/api/v2/"
# auth.oidc.additionalEndpointParams.var1 = "foobar"

# OIDC TLS and proxy configuration
# Specify a custom CA certificate file for verifying the OIDC token endpoint's TLS certificate.
# This is useful when the OIDC provider uses a self-signed certificate or a custom CA.
# auth.oidc.trustedCaFile = "/path/to/ca.crt"

# Skip TLS certificate verification for the OIDC token endpoint.
# INSECURE: Only use this for debugging purposes, not recommended for production.
# auth.oidc.insecureSkipVerify = false

# Specify a proxy server for OIDC token endpoint connections.
# Supports http, https, socks5, and socks5h proxy protocols.
# If not specified, no proxy is used for OIDC connections.
# auth.oidc.proxyURL = "http://proxy.example.com:8080"

# Set admin address for control frpc's action by http api such as reload
# webServer.addr = "127.0.0.1"
# webServer.port = 7400
# webServer.user = "admin"
# webServer.password = "admin"
# Admin assets directory. By default, these assets are bundled with frpc.
# webServer.assetsDir = "./static"

# Enable golang pprof handlers in admin listener.
webServer.pprofEnable = true

# The maximum amount of time a dial to server will wait for a connect to complete. Default value is 10 seconds.
transport.dialServerTimeout = 10

# dialServerKeepalive specifies the interval between keep-alive probes for an active network connection between frpc and frps.
# If negative, keep-alive probes are disabled.
transport.dialServerKeepalive = 600

# connections will be established in advance, default value is zero
transport.poolCount = 1000

# If tcp stream multiplexing is used, default is true, it must be same with frps
transport.tcpMux = true

# Specify keep alive interval for tcp mux.
# only valid if tcpMux is enabled.
transport.tcpMuxKeepaliveInterval = 30

# Communication protocol used to connect to server
# supports tcp, kcp, quic, websocket and wss now, default is tcp
transport.protocol = "tcp"

# FRP wire protocol used inside the selected transport.
# supports v1 and v2, default is v1. v2 requires frps support and must be enabled explicitly.
transport.wireProtocol = "v2"

# set client binding ip when connect server, default is empty.
# only when protocol = tcp or websocket, the value will be used.
# transport.connectServerLocalIP = "0.0.0.0"

# if you want to connect frps by http proxy or socks5 proxy or ntlm proxy, you can set proxyURL here or in global environment variables
# it only works when protocol is tcp
# transport.proxyURL = "http://user:passwd@192.168.1.128:8080"
# transport.proxyURL = "socks5://user:passwd@192.168.1.128:1080"
# transport.proxyURL = "ntlm://user:passwd@192.168.1.128:2080"

# quic protocol options
transport.quic.keepalivePeriod = 5 # 10
transport.quic.maxIdleTimeout = 60 # 30
transport.quic.maxIncomingStreams = 100000

# If tls.enable is true, frpc will connect frps by tls.
# Since v0.50.0, the default value has been changed to true, and tls is enabled by default.
# transport.tls.enable = true

# transport.tls.certFile = "client.crt"
# transport.tls.keyFile = "client.key"
# transport.tls.trustedCaFile = "ca.crt"
# transport.tls.serverName = "example.com"

# If skipServerNameVerify is set to true, frpc will skip TLS server name verification
# while still verifying the server's certificate chain. This is useful when the
# server certificate's CN/SAN doesn't match the connection hostname.
# When enabled, the certificate chain and expiration are still verified.
# transport.tls.skipServerNameVerify = false

# If the disableCustomTLSFirstByte is set to false, frpc will establish a connection with frps using the
# first custom byte when tls is enabled.
# Since v0.50.0, the default value has been changed to true, and the first custom byte is disabled by default.
# transport.tls.disableCustomTLSFirstByte = true

# Heartbeat configure, it's not recommended to modify the default value.
# The default value of heartbeatInterval is 10 and heartbeatTimeout is 90. Set negative value
# to disable it.
# transport.heartbeatInterval = 30
# transport.heartbeatTimeout = 90

# Specify a dns server, so frpc will use this instead of default one
# dnsServer = "8.8.8.8"

# Proxy names you want to start.
# Default is empty, means all proxies.
# This list is a global allowlist after config + store are merged, so entries
# created via Store API are also filtered by this list.
# If start is non-empty, any proxy/visitor not listed here will not be started.
# start = ["ssh", "dns"]

# Alternative to 'start': You can control each proxy individually using the 'enabled' field.
# Set 'enabled = false' in a proxy configuration to disable it.
# If 'enabled' is not set or set to true, the proxy is enabled by default.
# The 'enabled' field provides more granular control and is recommended over 'start'.

# Specify udp packet size, unit is byte. If not set, the default value is 1500.
# This parameter should be same between client and server.
# It affects the udp and sudp proxy.
udpPacketSize = 1300

# Feature gates allows you to enable or disable experimental features
# Format is a map of feature names to boolean values
# You can enable specific features:
#featureGates = { VirtualNet = true }

# VirtualNet settings for experimental virtual network capabilities
# The virtual network feature requires enabling the VirtualNet feature gate above
# virtualNet.address = "100.86.1.1/24"

# Additional metadatas for client.
#metadatas.var1 = "abc"
#metadatas.var2 = "123"

# Include other config files for proxies.
# includes = ["./confd/*.ini"]


# ============================================================================
# Proxy Configuration Examples
# ============================================================================
#
# Proxy types:
#   tcp   - Forward a remote TCP port on frps to a local TCP service.
#   udp   - Forward a remote UDP port on frps to a local UDP service.
#   http  - Route HTTP requests by Host header (vhost) to a local HTTP service.
#   https - Route HTTPS connections by SNI to a local service.
#   tcpmux - Multiplex TCP connections via HTTP CONNECT on a single frps port.
#   stcp  - Secret TCP: encrypted peer-to-peer tunnel relayed through frps.
#           No remotePort needed; visitors connect via shared secretKey.
#   xtcp  - P2P TCP: attempts direct NAT traversal between two frpcs,
#           falls back to frps relay if hole-punching fails.
#   sudp  - Secret UDP: encrypted peer-to-peer UDP tunnel relayed through frps.
#   socks5_relay - SOCKS5 relay exit node; frps routes SOCKS5 traffic to this
#           frpc based on user/group membership.
#
# Plugin types (replace localIP/localPort with a built-in handler):
#   unix_domain_socket - Forward TCP to a Unix domain socket.
#   http_proxy         - Expose an HTTP forward proxy.
#   socks5             - Expose a SOCKS5 proxy with optional auth.
#   static_file        - Serve local files over HTTP.
#   https2http         - Terminate TLS, forward as HTTP to local service.
#   https2https        - Re-encrypt TLS, forward as HTTPS to local service.
#   http2https         - Accept HTTP, forward as HTTPS to local service.
#   http2http          - Accept TCP, forward as HTTP to local service.
#   tls2raw            - Terminate TLS, forward raw TCP to local service.
#   virtual_net        - Inject traffic into a virtual network (requires feature gate).

# --- tcp: Forward frps:6001 -> 127.0.0.1:22 ------------------------------------
#[[proxies]]
# 'ssh' is the unique proxy name
# If global user is not empty, it will be changed to {user}.{proxy} such as 'your_name.ssh'
#name = "ssh"
#type = "tcp"
# Enable or disable this proxy. true or omit this field to enable, false to disable.
# enabled = true
#localIP = "127.0.0.1"
#localPort = 22
# Limit bandwidth for this proxy, unit is KB and MB
#transport.bandwidthLimit = "1MB"
# Where to limit bandwidth, can be 'client' or 'server', default is 'client'
#transport.bandwidthLimitMode = "client"
# If true, traffic of this proxy will be encrypted, default is false
#transport.useEncryption = false
# If true, traffic will be compressed
#transport.useCompression = false
# Remote port listen by frps
#remotePort = 6001
# frps will load balancing connections for proxies in same group
#loadBalancer.group = "test_group"
# group should have same group key
#loadBalancer.groupKey = "123456"
# Enable health check for the backend service, it supports 'tcp' and 'http' now.
# frpc will connect local service's port to detect it's healthy status
#healthCheck.type = "tcp"
# Health check connection timeout
#healthCheck.timeoutSeconds = 3
# If continuous failed in 3 times, the proxy will be removed from frps
#healthCheck.maxFailed = 3
# Every 10 seconds will do a health check
#healthCheck.intervalSeconds = 10
# Additional meta info for each proxy. It will be passed to the server-side plugin for use.
#metadatas.var1 = "abc"
#metadatas.var2 = "123"
# You can add some extra information to the proxy through annotations.
# These annotations will be displayed on the frps dashboard.
#[proxies.annotations]
#key1 = "value1"
#"prefix/key2" = "value2"

# --- tcp: remotePort = 0 lets frps auto-assign a port -------------------------
#[[proxies]]
#name = "ssh_random"
#type = "tcp"
#localIP = "192.168.31.100"
#localPort = 22
#remotePort = 0

# --- udp: Forward frps:6002(UDP) -> 114.114.114.114:53 -------------------------
#[[proxies]]
#name = "dns"
#type = "udp"
#localIP = "114.114.114.114"
#localPort = 53
#remotePort = 6002

# --- http: Route by Host header to local HTTP service ---------------------------
# Resolve your domain names to [serverAddr] so you can use
# http://web01.yourdomain.com to browse web01 and http://web02.yourdomain.com to browse web02
#[[proxies]]
#name = "web01"
#type = "http"
#localIP = "127.0.0.1"
#localPort = 80
# http username and password are safety certification for http protocol
# if not set, you can access this customDomains without certification
#httpUser = "admin"
#httpPassword = "admin"
# if domain for frps is frps.com, then you can access [web01] proxy by URL http://web01.frps.com
#subdomain = "web01"
#customDomains = ["web01.yourdomain.com"]
# locations is only available for http type
#locations = ["/", "/pic"]
# route requests to this service if http basic auto user is abc
# routeByHTTPUser = abc
#hostHeaderRewrite = "example.com"
#requestHeaders.set.x-from-where = "frp"
#responseHeaders.set.foo = "bar"
#healthCheck.type = "http"
# frpc will send a GET http request '/status' to local http service
# http service is alive when it return 2xx http response code
#healthCheck.path = "/status"
#healthCheck.intervalSeconds = 10
#healthCheck.maxFailed = 3
#healthCheck.timeoutSeconds = 3
# set health check headers
#healthCheck.httpHeaders=[
#    { name = "x-from-where", value = "frp" }
#]

# --- https: Route by SNI to local service --------------------------------------
#[[proxies]]
#name = "web02"
#type = "https"
# Disable this proxy by setting enabled to false
# enabled = false
#localIP = "127.0.0.1"
#localPort = 8000
#subdomain = "web02"
#customDomains = ["web02.yourdomain.com"]
# if not empty, frpc will use proxy protocol to transfer connection info to your local service
# v1 or v2 or empty
#transport.proxyProtocolVersion = "v2"

# --- tcpmux: Multiplex via HTTP CONNECT on a single frps port ------------------
#[[proxies]]
#name = "tcpmuxhttpconnect"
#type = "tcpmux"
#multiplexer = "httpconnect"
#localIP = "127.0.0.1"
#localPort = 10701
#customDomains = ["tunnel1"]
# routeByHTTPUser = "user1"

# --- plugin: unix_domain_socket - Forward TCP to a Unix socket -----------------
#[[proxies]]
#name = "plugin_unix_domain_socket"
#type = "tcp"
#remotePort = 6003
# if plugin is defined, localIP and localPort is useless
# plugin will handle connections got from frps
#[proxies.plugin]
#type = "unix_domain_socket"
#unixPath = "/var/run/docker.sock"

# --- plugin: http_proxy - Expose an HTTP forward proxy -------------------------
#[[proxies]]
#name = "plugin_http_proxy"
#type = "tcp"
#remotePort = 6004
#[proxies.plugin]
#type = "http_proxy"
#httpUser = "abc"
#httpPassword = "abc"

# --- plugin: socks5 - Expose a SOCKS5 proxy ------------------------------------
#[[proxies]]
#name = "plugin_socks5"
#type = "tcp"
#remotePort = 6005
#[proxies.plugin]
#type = "socks5"
#username = "abc"
#password = "abc"

# --- plugin: static_file - Serve local files over HTTP -------------------------
#[[proxies]]
#name = "plugin_static_file"
#type = "tcp"
#remotePort = 6006
#[proxies.plugin]
#type = "static_file"
#localPath = "/var/www/blog"
#stripPrefix = "static"
#httpUser = "abc"
#httpPassword = "abc"

# --- plugin: https2http - Terminate TLS, forward as HTTP ----------------------
#[[proxies]]
#name = "plugin_https2http"
#type = "https"
#customDomains = ["test.yourdomain.com"]
#[proxies.plugin]
#type = "https2http"
#localAddr = "127.0.0.1:80"
#crtPath = "./server.crt"
#keyPath = "./server.key"
#hostHeaderRewrite = "127.0.0.1"
#requestHeaders.set.x-from-where = "frp"

# --- plugin: https2https - Re-encrypt TLS, forward as HTTPS -------------------
#[[proxies]]
#name = "plugin_https2https"
#type = "https"
#customDomains = ["test.yourdomain.com"]
#[proxies.plugin]
#type = "https2https"
#localAddr = "127.0.0.1:443"
#crtPath = "./server.crt"
#keyPath = "./server.key"
#hostHeaderRewrite = "127.0.0.1"
#requestHeaders.set.x-from-where = "frp"

# --- plugin: http2https - Accept HTTP, forward as HTTPS -----------------------
#[[proxies]]
#name = "plugin_http2https"
#type = "http"
#customDomains = ["test.yourdomain.com"]
#[proxies.plugin]
#type = "http2https"
#localAddr = "127.0.0.1:443"
#hostHeaderRewrite = "127.0.0.1"
#requestHeaders.set.x-from-where = "frp"

# --- plugin: http2http - Accept TCP, forward as HTTP --------------------------
#[[proxies]]
#name = "plugin_http2http"
#type = "tcp"
#remotePort = 6007
#[proxies.plugin]
#type = "http2http"
#localAddr = "127.0.0.1:80"
#hostHeaderRewrite = "127.0.0.1"
#requestHeaders.set.x-from-where = "frp"

# --- plugin: tls2raw - Terminate TLS, forward raw TCP -------------------------
#[[proxies]]
#name = "plugin_tls2raw"
#type = "tcp"
#remotePort = 6008
#[proxies.plugin]
#type = "tls2raw"
#localAddr = "127.0.0.1:80"
#crtPath = "./server.crt"
#keyPath = "./server.key"

# --- stcp: Secret TCP — encrypted tunnel via frps, no remotePort needed -------
# Another frpc (visitor) connects to this service through frps using the same secretKey.
#[[proxies]]
#name = "secret_tcp"
#type = "stcp"
#secretKey = "abcdefg"
#localIP = "127.0.0.1"
#localPort = 22
# If not empty, only visitors from specified users can connect.
# Otherwise, visitors from same user can connect. '*' means allow all users.
#allowUsers = ["*"]

# --- xtcp: P2P TCP — direct NAT traversal, falls back to frps relay ----------
#[[proxies]]
#name = "p2p_tcp"
#type = "xtcp"
#secretKey = "abcdefg"
#localIP = "127.0.0.1"
#localPort = 22
# If not empty, only visitors from specified users can connect.
# Otherwise, visitors from same user can connect. '*' means allow all users.
#allowUsers = ["user1", "user2"]

# NAT traversal configuration (optional)
#[proxies.natTraversal]
# Disable the use of local network interfaces (assisted addresses) for NAT traversal.
# When enabled, only STUN-discovered public addresses will be used.
# This can improve performance when you have slow VPN connections.
# Default: false
#disableAssistedAddrs = false

# --- sudp: Secret UDP — encrypted UDP tunnel via frps --------------------------
#[[proxies]]
#name = "secret_udp"
#type = "sudp"
#secretKey = "abcdefg"
#localIP = "127.0.0.1"
#localPort = 53

# --- plugin: virtual_net — inject into virtual network (requires feature gate) -
#[[proxies]]
#name = "vnet-server"
#type = "stcp"
#secretKey = "your-secret-key"
#[proxies.plugin]
#type = "virtual_net"

# --- socks5_relay: SOCKS5 exit node for frps routing ---------------------------
# frpc registers as a SOCKS5 exit node; frps routes SOCKS5 requests to it
# based on user/group membership. Group comes from global 'group' or TLS cert OU.
[[proxies]]
name = "relay5"
type = "socks5_relay"
# outboundProxy = "optional-outbound-proxy-name"
# Token pool size: requests wait (block) when limit reached until a token is released
# 0 = unlimited, recommended: 3-10 for typical usage
maxConcurrent = 3


# ============================================================================
# Visitor Configuration Examples
# ============================================================================
#
# Visitors connect to stcp/xtcp/sudp proxy servers through frps.
# Each visitor binds a local port that applications can connect to.
#
# Flow: local app -> visitor:bindPort -> frps -> proxy server -> local service

# --- stcp visitor: connect to an stcp proxy via frps ---------------------------
#[[visitors]]
#name = "secret_tcp_visitor"
#type = "stcp"
#serverName = "secret_tcp"
#secretKey = "abcdefg"
#bindAddr = "127.0.0.1"
# bindPort can be less than 0, it means don't bind to the port and only receive
# connections redirected from other visitors. (Not supported for SUDP)
#bindPort = 9000

# --- xtcp visitor: P2P connection with NAT traversal --------------------------
#[[visitors]]
#name = "p2p_tcp_visitor"
#type = "xtcp"
# if the server user is not set, it defaults to the current user
#serverUser = "user1"
#serverName = "p2p_tcp"
#secretKey = "abcdefg"
#bindAddr = "127.0.0.1"
# bindPort can be less than 0, it means don't bind to the port and only receive
# connections redirected from other visitors. (Not supported for SUDP)
#bindPort = 9001
# when automatic tunnel persistence is required, set it to true
#keepTunnelOpen = false
# effective when keepTunnelOpen is set to true, the number of attempts to punch through per hour
#maxRetriesAnHour = 8
#minRetryInterval = 90
# fallbackTo = "stcp_visitor"
# fallbackTimeoutMs = 500

# NAT traversal configuration (optional)
#[visitors.natTraversal]
# Disable the use of local network interfaces (assisted addresses) for NAT traversal.
# When enabled, only STUN-discovered public addresses will be used.
# Default: false
#disableAssistedAddrs = false

# --- sudp visitor: connect to a sudp proxy via frps ----------------------------
#[[visitors]]
#name = "secret_udp_visitor"
#type = "sudp"
#serverName = "secret_udp"
#secretKey = "abcdefg"
#bindAddr = "127.0.0.1"
#bindPort = 9002

# --- plugin: virtual_net visitor — join virtual network ------------------------
#[[visitors]]
#name = "vnet-visitor"
#type = "stcp"
#serverName = "vnet-server"
#secretKey = "your-secret-key"
#bindPort = -1
#[visitors.plugin]
#type = "virtual_net"
#destinationIP = "100.86.0.1"

